{
  "name": "entropia-flow-client",
  "version": "0.2.0-dev.1",
  "description": "Entropia Flow Client",
  "main": "index.js",
  "scripts": {
    "build-stream": "cd ..\\chrome-extension\\ && npm install && npm run stream-dev && robocopy dist.win ..\\win-client-app\\resources\\stream /E /COPY:DAT /R:0 /W:0 || exit 0",
    "build-relay": "cd relay && rsrc -ico app.ico && go build -buildvcs=false -ldflags=\"-s -w\" .",
    "build": "webpack",
    "build-all": "npm run build-stream && npm run build-relay && npm run build && neu update && neu build --release",
    "zip": "node script/zip.js",
    "serve-update": "npx http-server . -p 8080 --cors -c-1",
    "pack": "npm run build-all && npm run zip"
  },
  "author": "Lucho MUCHO Ireton",
  "license": "MIT",
  "devDependencies": {
    "@types/mustache": "^4.2.6",
    "adm-zip": "^0.5.16",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.3",
    "webpack": "^5.100.2",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "mustache": "^4.2.0"
  }
}
